# System Architecture & Data Flow

## 📁 File Structure

```
aureus/
├── lib/
│   ├── userProfile.ts           # Core profile schema & analysis
│   ├── trackingEvents.ts        # Event tracking & storage
│   └── aiAdaptationEngine.ts    # AI features & profile storage
│
├── hooks/
│   ├── useUserProfile.ts        # Profile management hook
│   └── useSessionTracking.ts   # Timing utilities hook
│
├── components/
│   ├── PersonalizationExample.tsx      # Complete working example
│   └── UserTrackingDebugPanel.tsx      # Debug tools
│
└── docs/
    ├── TRACKING_SYSTEM.md         # Full API documentation
    ├── INTEGRATION_GUIDE.md       # Step-by-step tutorial
    ├── TRACKING_SUMMARY.md        # Overview & features
    ├── QUICK_REFERENCE.md         # Quick lookup guide
    └── ARCHITECTURE.md            # This file
```

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      Your React App                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    useUserProfile Hook                       │
│  • Auto-loads profile on mount                               │
│  • Auto-saves on updates                                     │
│  • Provides tracking methods                                 │
└─────────────────────────────────────────────────────────────┘
                              │
           ┌──────────────────┼──────────────────┐
           ▼                  ▼                  ▼
┌─────────────────┐ ┌──────────────────┐ ┌─────────────────┐
│  UserProfile    │ │ TrackingEvents   │ │  AI Adaptation  │
│  (Schema)       │ │ (Events)         │ │  (Intelligence) │
│                 │ │                  │ │                 │
│ • Structure     │ │ • trackWord()    │ │ • recommend()   │
│ • Analysis      │ │ • trackSession() │ │ • adapt()       │
│ • Predictions   │ │ • trackInteract()│ │ • insights()    │
└─────────────────┘ └──────────────────┘ └─────────────────┘
         │                   │                      │
         └───────────────────┼──────────────────────┘
                             ▼
                    ┌────────────────┐
                    │  localStorage  │
                    │                │
                    │ • user_profile │
                    │ • user_events  │
                    │ • user_id      │
                    └────────────────┘
```

## 🔄 Data Flow Diagrams

### Word Selection Flow

```
User selects words
       │
       ▼
┌─────────────────────────┐
│  Opening Screen         │
│  • selectedWords[]      │
│  • availableWords[]     │
│  • selectionDuration    │
└─────────────────────────┘
       │
       │ trackWordSelection()
       ▼
┌─────────────────────────┐
│  UserEventTracker       │
│  • Create event object  │
│  • Analyze clusters     │
│  • Analyze difficulty   │
│  • Store to localStorage│
└─────────────────────────┘
       │
       ▼
┌─────────────────────────┐
│  Profile Update         │
│  • Update clusters      │
│  • Add to word history  │
│  • Recalculate weights  │
│  • Save profile         │
└─────────────────────────┘
       │
       ▼
   Start Session
```

### Session Activity Flow

```
Session starts
       │
       ▼
┌─────────────────────────┐
│  useSessionTracking     │
│  • startSession()       │
│  • Track start time     │
└─────────────────────────┘
       │
       ▼
   Interaction begins
       │
       ▼
┌─────────────────────────┐
│  useSessionTracking     │
│  • startInteraction()   │
│  • Track mode used      │
└─────────────────────────┘
       │
       ▼
   User completes task
       │
       ▼
┌─────────────────────────┐
│  useSessionTracking     │
│  • completeInteraction()│
│  • Record time spent    │
│  • Record user input    │
└─────────────────────────┘
       │
       ▼
   More interactions...
       │
       ▼
   Session ends
       │
       ▼
┌─────────────────────────┐
│  getMetrics()           │
│  • Calculate totals     │
│  • Average times        │
│  • Engagement scores    │
└─────────────────────────┘
       │
       │ trackSessionCompletion()
       ▼
┌─────────────────────────┐
│  UserEventTracker       │
│  • Store event          │
│  • Update profile       │
│  • Adjust difficulty    │
│  • Update preferences   │
└─────────────────────────┘
```

### AI Recommendation Flow

```
User opens app
       │
       ▼
┌─────────────────────────┐
│  Load Profile           │
│  • Check word history   │
│  • Calculate patterns   │
└─────────────────────────┘
       │
       │ Has 10+ words?
       ├─ No  ─> Show default words
       │
       ▼ Yes
┌─────────────────────────┐
│  ProfileAnalyzer        │
│  • predictParams()      │
│  • Get recommended      │
│    word count           │
│  • Get difficulty range │
│  • Get priority clusters│
└─────────────────────────┘
       │
       ▼
┌─────────────────────────┐
│  AIAdaptationEngine     │
│  • Build AI prompt      │
│  • Include profile data │
│  • Send to Claude API   │
└─────────────────────────┘
       │
       ├─ API available?
       │
       ▼ Yes
┌─────────────────────────┐
│  Claude AI              │
│  • Analyze preferences  │
│  • Select optimal words │
│  • Balance challenge    │
│  • Return JSON          │
└─────────────────────────┘
       │
       ▼ No (or error)
┌─────────────────────────┐
│  Fallback Selection     │
│  • Filter by difficulty │
│  • Filter by clusters   │
│  • Shuffle & select     │
└─────────────────────────┘
       │
       ▼
   Display words to user
```

## 🧩 Component Relationships

```
┌──────────────────────────────────────────────────────────┐
│                    App (page.tsx)                         │
│                                                           │
│  ┌────────────────────────────────────────────────────┐  │
│  │        Opening Screen                               │  │
│  │  • useUserProfile()                                 │  │
│  │  • Profile stats display                            │  │
│  │  • AI recommendations                               │  │
│  │  • Word selection                                   │  │
│  │  • Journey insights                                 │  │
│  └────────────────────────────────────────────────────┘  │
│                         │                                 │
│                         │ User starts session             │
│                         ▼                                 │
│  ┌────────────────────────────────────────────────────┐  │
│  │        Session Screen                               │  │
│  │  • useSessionTracking()                             │  │
│  │  • Track each interaction                           │  │
│  │  • Collect metrics                                  │  │
│  │  • Complete session                                 │  │
│  └────────────────────────────────────────────────────┘  │
│                         │                                 │
│                         │ Session ends                    │
│                         ▼                                 │
│  ┌────────────────────────────────────────────────────┐  │
│  │        Analytics Screen                             │  │
│  │  • Display session results                          │  │
│  │  • Show updated profile                             │  │
│  │  • AI-generated insights                            │  │
│  └────────────────────────────────────────────────────┘  │
│                                                           │
│  ┌────────────────────────────────────────────────────┐  │
│  │   Debug Panel (dev only)                            │  │
│  │   • UserTrackingDebugPanel                          │  │
│  └────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

## 💾 Data Storage Schema

### localStorage Keys

```javascript
// User identification
'aureus_user_id': 'user_1733068800000_abc123'

// User profile
'user_profile:user_1733068800000_abc123': {
  userId: 'user_1733068800000_abc123',
  createdAt: '2025-12-01T12:00:00Z',
  updatedAt: '2025-12-01T15:30:00Z',
  vocabularyLevel: { /* ... */ },
  clusterPreferences: { /* ... */ },
  sessionPatterns: { /* ... */ },
  wordHistory: { /* ... */ },
  contextPreferences: { /* ... */ },
  learningStyle: { /* ... */ }
}

// User events (last 100)
'user_events:user_1733068800000_abc123': [
  {
    type: 'word_selection',
    userId: 'user_1733068800000_abc123',
    timestamp: '2025-12-01T12:00:00Z',
    data: { /* ... */ }
  },
  // ... more events
]
```

## 🔁 Profile Update Logic

```
Event triggers update
       │
       ▼
┌─────────────────────────┐
│  Event Type Check       │
└─────────────────────────┘
       │
       ├─ word_selection
       │  ├─> Update clusterPreferences
       │  ├─> Add to wordHistory
       │  └─> Recalculate weights
       │
       ├─ mini_session_completed
       │  ├─> Update sessionPatterns
       │  ├─> Update completionRate
       │  ├─> Update averageWords
       │  └─> Update learningStyle
       │
       └─ interaction
          ├─> Track mode usage
          └─> Update interactionDepth
```

## 🎨 AI Integration Points

```
┌────────────────────────────────────────────────┐
│           Your Application                      │
└────────────────────────────────────────────────┘
                     │
                     │ Needs personalization?
                     ▼
         ┌───────────────────────┐
         │  Check API Key        │
         │  Available?           │
         └───────────────────────┘
                     │
         ┌───────────┴───────────┐
         │                       │
       YES                      NO
         │                       │
         ▼                       ▼
┌─────────────────┐     ┌──────────────────┐
│  Claude AI      │     │  Rule-Based      │
│  • Smart        │     │  Fallback        │
│  • Contextual   │     │  • Fast          │
│  • Personalized │     │  • Offline OK    │
│  • Requires API │     │  • Simple logic  │
└─────────────────┘     └──────────────────┘
         │                       │
         └───────────┬───────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │  Return Results       │
         │  to Application       │
         └───────────────────────┘
```

## 🔍 Analysis Pipeline

```
Raw Profile Data
       │
       ▼
┌─────────────────────────┐
│  ProfileAnalyzer        │
│  • analyzeSelection()   │
│  • predictParams()      │
└─────────────────────────┘
       │
       ├─> Cluster Analysis
       │   • Count selections
       │   • Calculate dominance
       │   • Weight by recency
       │
       ├─> Difficulty Analysis
       │   • Track averages
       │   • Identify trends
       │   • Find sweet spot
       │
       ├─> Pattern Analysis
       │   • Session lengths
       │   • Completion rates
       │   • Time preferences
       │
       └─> Context Analysis
           • Detect preferences
           • Weight by engagement
           • Predict focus areas
       │
       ▼
┌─────────────────────────┐
│  Actionable Insights    │
│  • recommendedWords: 7  │
│  • difficulty: 5-8      │
│  • clusters: [....]     │
│  • context: 'work'      │
└─────────────────────────┘
```

## 🛠️ Error Handling Strategy

```
Try AI Feature
       │
       ▼
┌─────────────────┐
│  Check API Key  │
└─────────────────┘
       │
       ├─ No key? ──────> Use fallback
       │
       ▼ Has key
┌─────────────────┐
│  Make API Call  │
└─────────────────┘
       │
       ├─ Network error? ──> Use fallback
       ├─ Parse error? ────> Use fallback
       ├─ Rate limit? ─────> Use fallback
       │
       ▼ Success
┌─────────────────┐
│  Return Result  │
└─────────────────┘

Fallback Strategy:
✓ Always available
✓ No external dependencies
✓ Fast and reliable
✓ Graceful degradation
```

## 📊 Performance Considerations

```
┌─────────────────────────────────────────┐
│  Performance Optimizations              │
├─────────────────────────────────────────┤
│  ✓ Async operations don't block UI      │
│  ✓ localStorage ops are fast (<5ms)     │
│  ✓ Events auto-prune to last 100        │
│  ✓ AI calls are optional                │
│  ✓ Profile loads once on mount          │
│  ✓ Updates batched to avoid spam        │
│  ✓ Analysis runs on-demand              │
│  ✓ TypeScript compiled (no runtime)     │
└─────────────────────────────────────────┘
```

## 🔐 Privacy & Security

```
┌─────────────────────────────────────────┐
│  Privacy Features                        │
├─────────────────────────────────────────┤
│  ✓ Data stored locally by default       │
│  ✓ No automatic cloud sync              │
│  ✓ User IDs are random                  │
│  ✓ Export/delete capabilities           │
│  ✓ No PII collected                     │
│  ✓ GDPR-friendly                        │
│  ✓ Clear data anytime                   │
└─────────────────────────────────────────┘
```

## 🚀 Migration Path to Backend

```
Current (localStorage)
       │
       ▼
┌─────────────────────────┐
│  1. Create API Routes   │
│  • POST /api/profile    │
│  • GET /api/profile/:id │
│  • PUT /api/profile/:id │
│  • POST /api/events     │
└─────────────────────────┘
       │
       ▼
┌─────────────────────────┐
│  2. Update Storage      │
│  • Replace localStorage │
│  • Use fetch() instead  │
│  • Keep same interface  │
└─────────────────────────┘
       │
       ▼
┌─────────────────────────┐
│  3. Add Authentication  │
│  • NextAuth.js          │
│  • Link profiles        │
│  • Multi-device sync    │
└─────────────────────────┘
       │
       ▼
   Production Ready!
```

---

**System is fully documented and ready for integration** ✅
